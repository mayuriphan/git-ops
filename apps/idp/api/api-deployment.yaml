apiVersion: apps/v1
kind: Deployment
metadata:
  name: idp-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: idp-api
  template:
    metadata:
      labels:
        app: idp-api
    spec:
      containers:
        - name: api
          image: 187457215475.dkr.ecr.ap-south-1.amazonaws.com/idp-api:79cfd26
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: idp-config
            - secretRef:
                name: idp-secrets
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
